pipeline {
    agent { label 'master' }
        stages {
	        stage('checkout'){
		            steps {
			                   bat 'svn co http://localhost:8020/svn/ibm'
					               }
						               }

								           }
									   }

